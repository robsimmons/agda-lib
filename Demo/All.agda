{- A bit of a regression test; at least the demos should always work -}

module Demo.All where

module ARITH where
   open import Demo.Arith

module LOGIC where
   open import Demo.Logic

module STLC where
   open import Demo.STLC

module SPINE-FORM where
   open import Demo.SpineForm

module SUBST where
   open import Demo.Subst